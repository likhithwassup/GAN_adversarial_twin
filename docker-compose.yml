services:
  dashboard:
    build: ./blue_team
    volumes:
      - ./blue_team:/app
      - ./shared_battleground:/app/shared_battleground
    # ... keep the rest the same ...

  orchestrator:
    build: ./blue_team
    volumes:
      - ./blue_team:/app
      - ./shared_battleground:/app/shared_battleground
    # ... keep the rest the same ...